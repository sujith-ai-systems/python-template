[project]
name = "python-template"
version = "0.1.0"
description = "A standardized enterprise Python template"
requires-python = ">=3.12"
dependencies = []

[tool.black]
line-length = 88

[tool.ruff]
line-length = 88
select = ["E", "F", "I"] # Enforce style, syntax, and clean import sorting
